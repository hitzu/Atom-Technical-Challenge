rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Scaffold-only: open rules for local emulator usage.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

