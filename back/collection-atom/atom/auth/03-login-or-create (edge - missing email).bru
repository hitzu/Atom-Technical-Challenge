meta {
  name: 03-login-or-create (edge - missing email)
  type: http
  seq: 3
}

post {
  url: {{base_url}}/auth/login-or-create
  body: json
  auth: none
}

body:json {
  {
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

