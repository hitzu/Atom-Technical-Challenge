meta {
  name: 02 create (edge - invalid email)
  type: http
  seq: 2
}

post {
  url: {{base_url}}/auth/login-or-create
  body: json
  auth: none
}

body:json {
  {
    "email": "not-an-email"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
