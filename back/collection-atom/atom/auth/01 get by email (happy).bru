meta {
  name: 01 get by email (happy)
  type: http
  seq: 4
}

get {
  url: {{base_url}}/users/:email
  body: json
  auth: none
}

params:path {
  email: 2%40gmail.com
}

vars:pre-request {
  email: robert.torres.lopez@gmail.com
}

script:post-response {
  if (res.status === 200 && res.body?.token) {
    bru.setEnvVar('token', res.body.token);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
