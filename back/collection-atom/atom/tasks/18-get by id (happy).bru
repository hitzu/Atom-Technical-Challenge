meta {
  name: 18-get by id (happy)
  type: http
  seq: 18
}

get {
  url: {{base_url}}/tasks/{{task_id}}
  auth: none
}

headers {
  authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
