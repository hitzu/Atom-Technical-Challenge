meta {
  name: 09-update (edge - non-existent id)
  type: http
  seq: 9
}

patch {
  url: {{base_url}}/tasks/{{nonExistingTaskId}}
  body: json
  auth: none
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "completed": true
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

