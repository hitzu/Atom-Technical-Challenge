meta {
  name: 10-delete (happy)
  type: http
  seq: 10
}

delete {
  url: {{base_url}}/tasks/{{taskId}}
  body: json
  auth: none
}

headers {
  authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

