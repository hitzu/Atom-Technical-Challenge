meta {
  name: 01-list (happy)
  type: http
  seq: 1
}

get {
  url: {{base_url}}/tasks?sort=desc
  body: json
  auth: none
}

params:query {
  sort: desc
}

headers {
  authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
