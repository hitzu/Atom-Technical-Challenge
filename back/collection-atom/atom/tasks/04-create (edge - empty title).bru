meta {
  name: 04-create (edge - empty title)
  type: http
  seq: 4
}

post {
  url: {{base_url}}/tasks
  body: json
  auth: none
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "title": ""
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

