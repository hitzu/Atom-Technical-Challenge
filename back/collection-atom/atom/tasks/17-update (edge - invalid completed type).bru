meta {
  name: 17-update (edge - invalid completed type)
  type: http
  seq: 17
}

patch {
  url: {{base_url}}/tasks/{{taskId}}
  body: json
  auth: none
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "completed": "true"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

