{
  "name": "todo-challenge-monorepo",
  "private": true,
  "workspaces": [
    "front",
    "back",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -k -n front,back \"npm run dev:front\" \"npm run dev:back\"",
    "dev:front": "npm run start --workspace front",
    "dev:back": "npm run start:dev --workspace back",
    "build": "npm run build --workspace back && npm run build --workspace front",
    "test": "npm test --workspace back && npm test --workspace front",
    "lint": "npm run lint:back && npm run lint:front",
    "lint:back": "npm run lint --workspace back",
    "lint:front": "eslint \"front/src/**/*.{ts,tsx}\" --max-warnings=0"
  },
  "devDependencies": {
    "@angular/compiler": "^17.3.0",
    "@typescript-eslint/eslint-plugin": "^8.54.0",
    "@typescript-eslint/parser": "^8.54.0",
    "concurrently": "^9.2.1",
    "eslint": "^9.39.2"
  }
}
